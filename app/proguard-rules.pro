# Minification is off for release today (isMinifyEnabled = false), so this file
# is a placeholder. Turning it on will need keep rules for kotlinx.serialization
# serializers and for any Retrofit interface reached reflectively.
